---
import { cn } from "@/lib/utils";
import logo from "@/assets/logo.svg";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<img
  src={logo.src}
  alt=""
  aria-hidden="true"
  class={cn("size-full rounded border object-contain p-0.5 invert", className)}
/>
